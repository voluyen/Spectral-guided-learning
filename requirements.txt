# Server (GPU): install torch with matching CUDA first, then this file.
# Local (CPU dev): pip install --index-url https://download.pytorch.org/whl/cpu torch
torch>=2.6
transformers>=4.56,<5   # Qwen3 support; <5 avoids v5's TrainingArguments.warmup_ratio removal
peft>=0.13              # optional LoRA fine-tuning path in train_sft.py (--use-lora)
deepspeed>=0.15         # optional --deepspeed-config path in train_sft.py (ZeRO-2 + CPU offload)
datasets>=2.20
accelerate>=1.0
numpy>=2.0,<2.5         # <2.5 required by vllm's pinned numba==0.65.0 dep (see setup.sh)
pandas>=2.2
pyarrow
pyyaml
tqdm
math-verify
json-repair             # tolerant JSON parsing of prucot_prune.py's LLM pruning-agent replies
pytest
# GPU-server only: flash-attn and vllm are installed by scripts/setup.sh directly, not from here.
