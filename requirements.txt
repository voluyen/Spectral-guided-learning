# Server (GPU): install torch with matching CUDA first, then this file.
# Local (CPU dev): pip install --index-url https://download.pytorch.org/whl/cpu torch
torch>=2.6
transformers>=4.56      # Qwen3 support; `dtype=` arg (replaces torch_dtype)
datasets>=2.20
accelerate>=1.0
numpy>=2.0              # pandas wheels are built against numpy 2; numpy 1.x raises a dtype ABI error
pandas>=2.2
pyarrow
pyyaml
math-verify
pytest
# GPU-server only (skip on local CPU machine):
# flash-attn        # optional, SDPA fallback supported via config
# vllm>=0.8         # eval generation
